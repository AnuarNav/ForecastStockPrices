"""This module compares the returns of each portfolio with it's time equivalent predicted portfolio.
Result format:
| Strategy (Markowitz/Sharpe) | Start_date | end_date | return | return_with_prediction | pct_change"""